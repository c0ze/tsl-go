# Built binaries
/tsl-go
/tsl-go.exe
/dist/

# Original 0.40 build artifacts (built inside tsl-0.40/)
tsl-0.40/tsl_console
tsl-0.40/tsl_gui

# Runtime artifacts
morgue.txt
.tsl-save.json
tsl_conf
/tsl-wasm

# Music: keep the original WAVs locally but out of git. The committed, deployed
# assets are web/audio/*.mp3 (regenerate with scripts/encode-audio.sh).
/assets/audio/src/

# Local web build artifacts — CI rebuilds these (.github/workflows/pages.yml).
/web/tsl-go.wasm
/web/wasm_exec.js

# Local tooling and OS cruft
.DS_Store
/.claude/
